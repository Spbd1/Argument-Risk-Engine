services:
  backend:
    build: .
    command: python scripts/run_backend.py
    ports: ["8000:8000"]
  frontend:
    image: node:20-alpine
    working_dir: /app/frontend
    volumes: [".:/app"]
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    ports: ["5173:5173"]
